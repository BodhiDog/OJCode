#include <bits/stdc++.h>
using namespace std;

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	cout.tie(nullptr);
	double r;
	cin >> r;
	cout << fixed << setprecision(6) << 3.14159265358979323 * r * r << '\n'
		 << 2 * r * r << flush;
	return 0;
}
